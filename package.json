{
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r -parallel run build",
    "start": "pnpm -r -parallel run start",
    "dev": "pnpm -r -parallel run dev"
  }
}
